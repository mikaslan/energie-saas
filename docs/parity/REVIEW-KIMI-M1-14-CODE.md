kimi version 0.40.1
• Let me analyze this task. I'm asked to review a code diff for slice M1-14 (Kontaktdatensatz, Migration 0042, F1.5) against its spec. The review is quality-focused: security (RLS/roles/grants/pins), correctness vs spec, erasure, races/locks, DTO minimization, discipline.

  Important: "Bewerte nur den Diff; keine Dateioperationen." — I should NOT do file operations. The diff is provided inline. I need to review the diff as given.

  Note: The diff says `contact-actions.ts` and `contact-section.tsx` are listed as DIFF headers but their content is NOT included in the diff (only page.tsx diff content is shown, plus new files). Wait, looking at the top:

  ```
  === DIFF: app/w/[workspaceId]/anfragen/[projectId]/contact-actions.ts ===
  === DIFF: app/w/[workspaceId]/anfragen/[projectId]/contact-section.tsx ===
  === DIFF: app/w/[workspaceId]/anfragen/[projectId]/page.tsx ===
  diff --git a/app/w/[workspaceId]/anfragen/[projectId]/page.tsx ...
  ```

  So contact-actions.ts and contact-section.tsx are empty diffs in what I received — their contents are not shown. That's a gap I should note: the server action wiring (contact-actions.ts) is not in the diff, so I can't verify error mapping at the action boundary, re-auth, etc. That's a review limitation, possibly a P1/P2 note: the action file is referenced but not shown — cannot verify. Actually the instruction says "Bewerte nur den Diff" — so I should flag that these files' contents are missing from the provided diff.

  Let me now carefully review what's in the diff.

  ### 1. Security: RLS/Rollen-Grants und Pins

  `scripts/db-role-contract.mts`:
  - Adds `"contact_name_split_v1:app_owner"` to a function ownership list.
  - Adds a pinned function descriptor: `"contact_name_split_v1(text):TABLE(first_name text, last_name text):app_owner:sql:f:i:false:false:false:u:search_path=pg_catalog:0ff6..."`.
    - Fields appear to be: name(args):return:owner:lang:...:security definer flags...:search_path:hash. The `f:i:false:false:false:u` — presumably `prosecdef=false` (not SECURITY DEFINER). Good: it's a plain SQL IMMUTABLE function with search_path pinned.
    - The erase_inactive_lead hash is updated: old `891d...` → new `742a...` (conditional on hasCustomerNotification && hasProjectNotes). The migration itself pins the expected old hash `891d...` before replacing — consistent. Good: no pin removal without justification; the new pin matches the migration's output presumably (we can't verify the hash ourselves without running, but structure is consistent — the migration raises if the pre-image hash doesn't match `891d...`, and the contract now expects `742a...`).

  Migration 0042:
  - `CREATE FUNCTION public.contact_name_split_v1(raw_name text) RETURNS TABLE ... LANGUAGE sql IMMUTABLE SET search_path = pg_catalog` — good, not SECURITY DEFINER.
  - `REVOKE ALL ON FUNCTION ... FROM PUBLIC;` — good.
  - Then `DO $m114_erasure_acl$` revokes EXECUTE from a list of roles including app_runtime, and grants EXECUTE on erase_inactive_lead to app_erasure.

  Wait — issue: `contact_name_split_v1` is created for the **backfill**; the TS-side `contactNameSplitV1` is used by intake/edit. So the SQL function is only needed at migration time. Revoking EXECUTE from everyone is fine. But does `app_migrator` need EXECUTE during the migration? The migration runs as the migrator/owner — the REVOKE happens in a later DO block after the backfill, so within the migration the function is usable. Order: CREATE FUNCTION → REVOKE FROM PUBLIC → backfill (uses the function) → erasure upgrade DO → ACL DO. Wait, let me re-check order in the migration file:

  1. ALTER TABLE add columns + checks.
  2. CREATE FUNCTION contact_name_split_v1.
  3. REVOKE ALL FROM PUBLIC.
  4. Backfill block: LOCK TABLE, NO FORCE RLS, UPDATE using contact_name_split_v1, SET NOT NULL, FORCE RLS.
  5. Erasure upgrade DO block.
  6. ACL DO block: revoke EXECUTE on contact_name_split_v1 from all app roles; grant EXECUTE erase_inactive_lead to app_erasure.

  Hmm — does the migration run as `app_migrator` or as owner (`app_owner`)? The role contract lists `contact_name_split_v1:app_owner` as owner. The migration presumably runs as a role that owns the function (owner bypasses ACL). Since default EXECUTE is granted to PUBLIC, and revoked from PUBLIC at step 3, subsequent use by owner is fine. OK.

  Potential issue: The ACL block revokes EXECUTE from roles `IF to_regrole(...) IS NOT NULL` — fine, idempotent.

  Grant on erase_inactive_lead to app_erasure — re-grant after CREATE OR REPLACE? CREATE OR REPLACE preserves existing grants actually... In Postgres, CREATE OR REPLACE FUNCTION preserves the existing grants (ownership and privileges are retained). So the re-grant is belt-and-braces; harmless. Actually wait — CREATE OR REPLACE does preserve privileges. So the GRANT is redundant but harmless. Not a finding.

  Bigger security consideration: The migration temporarily does `ALTER TABLE public.contact NO FORCE ROW LEVEL SECURITY` for the backfill, guarded by `LOCK TABLE ... IN ACCESS EXCLUSIVE MODE`. The comment says only the table owner sees rows in that window. Running migrations while app_runtime serves traffic: NO FORCE RLS doesn't grant app_runtime more access — FORCE RLS applies to the table owner; app_runtime is subject to RLS either way. NO FORCE RLS means the *owner* bypasses RLS. app_runtime is not the owner, so RLS still applies to app_runtime regardless of FORCE. So no runtime exposure. Good. And LOCK TABLE ACCESS EXCLUSIVE serializes. OK.

  But wait — is `ALTER TABLE ... NO FORCE ROW LEVEL SECURITY` even necessary? The backfill runs as owner; with FORCE RLS, the owner is also subject to policies. If policies restrict visibility to current workspace via `app.workspace_id` GUC, the migrator (without the GUC) would see zero rows, and the UPDATE would update zero rows. So the NO FORCE step is required. Fine. And it restores FORCE after. If the migration fails between, FORCE RLS stays off — a partial-migration concern, but migrations failing leave the DB in a failed state anyway (transactional DDL in Postgres — if run in a transaction, rollback restores). Drizzle migrations: are they wrapped in a transaction? The `--> statement-breakpoint` markers suggest drizzle-kit generated, and migrate.mts may run each statement separately or in a transaction. `LOCK TABLE ... IN ACCESS EXCLUSIVE MODE` outside a transaction would be released at session end. If the migration runner wraps the whole file in one transaction, failure rolls back everything including NO FORCE. Can't verify from the diff — but drizzle migrations in this project presumably run transactionally (prior migrations use similar patterns, e.g., 0027 with advisory locks and the M1-11b source-pinned replacement pattern — "Muster 0039/0041"). I'll not flag this, or maybe a minor P2 note? The task says review quality, and this pattern matches existing migrations. Skip.

  Erasure upgrade: The DO block reads prosrc, checks SHA-256 equals `891d...` (the M1-13 state), checks anchor presence, replaces old scrub with new scrub, CREATE OR REPLACE with `SECURITY DEFINER SET search_path = pg_catalog`. Good: SECURITY DEFINER preserved with search_path pinned to pg_catalog.

  One issue: `CREATE OR REPLACE FUNCTION ... AS %L` with `upgraded_source` — prosrc from pg_proc. prosrc in modern Postgres stores the original source text. Replacing the anchor substring works if the stored prosrc matches byte-for-byte the anchor in the migration — the hash pin ensures prosrc is exactly the expected M1-13 source, so the anchor matches. Good — the hash check makes the strpos anchor check almost redundant, but defense in depth. OK.

  Another subtlety: `upgraded_source := replace(erase_source, old, new)` — replace replaces ALL occurrences; if the anchor appeared twice it would double-replace. Given hash-pinned source, fine.

  The new erase hash in db-role-contract: `742a9a4e...` — conditional branch: `hasCustomerNotification && hasProjectNotes ? "742a..." : hasCustomerNotification ? "2665..." : ...`. The migration raises unless the pre-image is `891d...` — which corresponds to the state where both customer-notification and project-notes exist (the old contract expected `891d...` in that branch). Consistent: the migration only supports the both-features state. But what about legacy migration paths where the DB does NOT have customer-notification or project notes? Then `erase_inactive_lead` has a different hash and migration 0042 would abort. The spec says "Fresh- + Legacy-Migrationspfad" as gate. The role-contract still carries the older hashes for other branch combos — but those branches would never get 0042 applied? Hmm, actually the hashes `2665...` etc. are for DBs at earlier states — but after 0042 is applied in a legacy path, the hash would be the new scrub variant too... The migration hardcodes expected pre-hash `891d...`. If a legacy path (without customer notification) reaches 0042, it aborts. But M1-13 (0041) presumably brings every path to the same state first — the role contract's conditional branches exist because feature-detection queries the DB schema state. After full migration to 0042, all paths have notification + notes, so the first branch applies. Consistent.

  BUT: there's a subtle mismatch — the contract's new hash `742a...` is in the `hasCustomerNotification && hasProjectNotes` branch. After 0042, erase_inactive_lead's prosrc changes, so hash differs from `891d...`. The contract expects `742a...`. We cannot verify that `742a...` is the actual SHA-256 of the replaced source without running. The prompt says the technical gate is green (db:generate no drift, role probe 88/88+5/5), so the role-contract verification passed — meaning the hash matched. OK, trust that.

  Now — the grants on the contact table itself. The diff doesn't show any new GRANT on table contact for app_runtime. Existing grants presumably already cover contact (SELECT/INSERT/UPDATE for app_runtime since intake writes contacts and project pages read them). The role contract count stays 88/88 — the diff to db-role-contract only adds the function pins, no table-grant changes. Since contact table existed with runtime grants (intake inserts via app_runtime presumably... actually intake insert runs under which role? The Rechner intake likely runs as app_runtime or a dedicated role). Anyway — no broadening of table grants in the diff. Good: app_runtime stays minimal — no new table reads granted.

  Hmm, but one point: `contact_name_split_v1` EXECUTE revoked from app_runtime — but intake service uses the **TS** implementation, not the SQL function, so runtime doesn't need EXECUTE. Consistent. Good.

  ### 2. Correctness vs Spec

  **Command validation**: `contactUpdateCommandV1Schema` — strictObject with schemaVersion literal, projectId uuid (transformed to lowercase — note: `uuidSchema` transforms to lowercase but the command uses non-canonical schema; the dataset uses canonicalUuidSchema), expectedRevision 1..2^31-1, patch strict allowlist with refine non-empty. Good.

  Wait — issue: `contactUpdateCommandV1Schema` does NOT include `workspaceId`. The spec says `updateContact({ workspaceId, projectId, expectedRevision, patch })`. The service signature is `updateContact(tx, ctx, input)` where workspaceId comes from ctx (authorized tenant context). The page-level server action (contact-actions.ts — not shown) presumably calls `authorizedAction(workspaceId, "contact.write", ...)`. The command schema lacking workspaceId is fine since ctx provides it. OK.

  **Patch validation details**:
  - `emailSecondarySchema`: z.string().min(3).max(254) + superRefine regex `^[^@\s]+@[^@\s]+$`. Service normalizes `lower(trim)`. But: the schema validates the RAW value (pre-normalization). `" Sekundaer@Example.com "` with spaces would fail the regex (`\s` excluded) — good. But uppercase passes raw; then service lowercases. DB CHECK requires `email_secondary = lower(btrim(email_secondary))` — service normalizes, fine.
  - However: `trimOrNull` in service — for `emailSecondary`, service calls `normalizeEmailSecondary` which does trim+lower. For a raw value `"  x@y.de  "` the schema regex would reject (leading space) — fine.
  - Edge: schema min(3) — `a@b` passes regex and length 3. DB CHECK length between 3 and 254. OK.
  - `phoneE164Schema`: `^\+[1-9][0-9]{1,14}$` matches DB CHECK. Good.
  - `postalCodeSchema`: trimmedText(1,20) — free form only; the DE-coupling is enforced only at DB CHECK. The contract test explicitly documents this (`"ABC123"` with `DE` passes contract, DB would reject). Divergence between contract validation and DB CHECK: an editor submitting PLZ "ABC123" + country "DE" passes Zod but fails the DB CHECK → the UPDATE raises a CHECK violation → unhandled error type! The service catches nothing around the UPDATE; a CHECK violation would propagate as a raw DB error. Fehlerklassen spec: invalid/not_found/conflict/deleted_contact/unauthenticated/denied; "Unbekannte Fehler nicht roh nach außen" — the action layer (contact-actions.ts, not shown) must map. But this is a UX/correctness gap: the contract test even codifies the divergence. Spec §10.1 M114-CONTRACT-03 says "PLZ-Muster ^[0-9]{5}$ nur bei address_country IN ('DE', null)" — the contract test title claims it tests that, but the implementation explicitly does NOT couple it in Zod ("freie Form wird im Contract nicht gekoppelt"). So the test named M114-CONTRACT-03 doesn't actually verify the spec's coupling; the coupling exists only in the DB. A client submitting German address with bad PLZ gets a DB CHECK violation instead of a validation error. That's a P2 (or P1?) — the UI would show a generic error rather than a field error. Spec §9 wants conflict/error states preserving draft; a raw CHECK violation mapped to a generic error is acceptable fail-closed behavior but poor. I'd call it P2: validation coupling missing at contract level; DB catches it fail-closed; risk is a 500-ish generic error path. Also note the service doesn't translate DB CHECK violations into `invalid` — potential internals leak depends on the action boundary (not shown). Since contact-actions.ts isn't in the diff, I can't verify the error mapping. P2 with note.

  - **Consent-CHECK (M114-CONTRACT-04)**: Spec §10.1: "Consent-CHECK (true ⇒ Version/At/Source gesetzt)". Looking at the migration: there is NO new consent CHECK added in 0042! The existing schema has a check on marketing_consent_source (the pre-existing `marketing_consent` CHECK visible in crm.ts context lines: `marketing_consent_source ... length(btrim(...)) > 0` — that's an existing CHECK requiring source set when...? The context shows an existing constraint around marketingConsentSource). The spec's M114-CONTRACT-04 says "Consent-CHECK (true ⇒ Version/At/Source gesetzt)" — the contract test named M114-CONTRACT-04 only tests policyVersion + https link, NOT a consent-true-implies-version invariant. And there's no DB CHECK tying `marketing_consent = true ⇒ marketing_consent_policy_version IS NOT NULL`. Hmm. Is that required? Spec §3.2 M114-06: "bool + Version + Text + Link + At + Source; Consent-CHECK". The diff adds no such CHECK. The existing CHECK (visible in context of crm.ts) appears to be `contact_marketing_consent_source_ck` presumably: `(marketing_consent = false ... or source is not null ...)` — we see fragment: `and length(btrim(${t.marketingConsentSource})) > 0`. That's the pre-existing consent-source CHECK. The policy_version is NOT tied to consent=true. 

    Is this a violation? Spec M114-CONTRACT-04: "Consent-CHECK (true ⇒ Version/At/Source gesetzt)". If interpreted strictly, the implementation misses a CHECK `marketing_consent ⇒ policy_version is not null`. But wait — the patch allowlist does NOT include `marketingConsent` boolean itself ("Primär-E-Mail/Festnetz und der Consent-Boolean (At/Source) sind intake-owned und nicht Teil des Patches"). So consent=true only happens at intake, where policy_version isn't set (intake doesn't set policy version!). Intake sets `marketingConsent: false` always (per diff: `marketingConsent: false`). Hmm, actually the intake diff shows `marketingConsent: false` — so consent true never set via intake either? Then where does consent get granted? Perhaps existing intake code sets marketingConsent based on payload — the diff shows `marketingConsent: false,` in the values — that might be pre-existing (context shows it was there). Actually the diff context shows `marketingConsent: false` unchanged. So consent granting may happen elsewhere or not at all currently.

    Given the contract test M114-CONTRACT-04 in the diff only checks policyVersion format + https link, and the spec text for M114-CONTRACT-04 says "Consent-CHECK (true ⇒ Version/At/Source gesetzt)" — there IS a gap: no CHECK ties consent=true to policy_version. But is that a P1? The DB cannot reasonably enforce `consent ⇒ policy_version` if policy version is WMEE-extension with unknown source (UNK-M114-03: Startwert v1/leer ist ESTIMATE). Hmm — but spec explicitly lists the CHECK in its own test matrix. The test as written doesn't test what the spec says. That's a spec-conformance finding: P1 (spec test not implemented as specified) or P2. Since marketing consent is compliance-relevant (DSGVO), and the spec's own matrix demands the CHECK, I'd flag P1: the Consent-CHECK `true ⇒ Version/At/Source` is not implemented (neither DB-CHECK noch Zod-Refinement), and M114-CONTRACT-04 tests something weaker. Mitigation: either add CHECK/refinement or amend spec. Actually wait — let me re-read: spec §10.1 `M114-CONTRACT-04`: "Consent-CHECK (true ⇒ Version/At/Source gesetzt)". And M114-06 in §3.2: "bool + Version + Text + Link + At + Source; Consent-CHECK". So yes, spec demands it. The existing DB has a CHECK on source (pre-existing, covering "Source"), but Version is new and not covered. Flag as P1 (compliance-adjacent, spec-deviation), with fix: add DB CHECK `marketing_consent = false or (marketing_consent_policy_version is not null and marketing_consent_at is not null and marketing_consent_source is not null)` — hmm but careful: existing rows with consent=true and no policy version? Backfill doesn't set policy_version; if legacy rows have marketing_consent=true, adding the CHECK would fail on existing data unless backfilled or the CHECK is NOT VALID. The migration doesn't touch it. So fix needs care (NOT VALID + validate or conditional). Flag P1 with that nuance.

    Hmm, wait. Let me reconsider severity. The gate is green; this is a spec-conformance gap in a compliance field. The service patch can't set `granted` at all, so through M1-14's own write path you cannot create consent=true-without-version. The only writers of marketing_consent=true are intake (sets false) and... possibly existing M1-04 intake logic sets it from payload consent flag (the diff shows `marketingConsent: false` literally — so no). Actually maybe there are other paths (e.g., original intake in modules/intake sets from payload.customer.consent?). The diff shows `marketingConsent: false,` in the values — that might be pre-existing (context shows it was there). Actually the diff context shows `marketingConsent: false` unchanged. So consent granting may happen elsewhere or not at all currently.

    I'll flag it as P1-lite/P2+? Let me decide: Spec test matrix is a contract; M114-CONTRACT-04 as specified is not implemented (CHECK missing; test weaker). That's a real spec deviation. I'll make it P1 because DSGVO-consistency (Nachweisbarkeit) is the stated purpose of policy_version (ADR Entsch. 4: "DSGVO verlangt Nachweisbarkeit, welche Policy-Version wann galt") — a consent=true without version defeats that. P1.

  - **display_name convention**: Service `deriveDisplayName(first, last) = \`${first} ${last}\`.trim()`. Spec: `btrim(first_name || ' ' || last_name)`. Match. But note: the service writes display_name with a raw template — firstName/lastName are Zod-validated min(1).max(200) but NOT required to be trimmed or whitespace-collapsed! `nameSchema = z.string().min(1).max(200)` — no trim refine, no whitespace normalization. So patch could contain firstName "  Erika  " → service `trimOrNull`? Wait, in the update loop: `typeof value === "string" → normalized = trimOrNull(value)` — so firstName gets trimmed (but internal whitespace not collapsed). display_name = "Erika Musterfrau" from trimmed parts. OK. But DB CHECK on first_name is `length(btrim(first_name)) between 1 and 200` — btrim only trims ends. "Erika  Marie" as firstName passes (trimmed ends, internal spaces allowed). Spec's split convention concerns display→split, not the reverse. Fine.

    But here's a subtle inconsistency: nameSchema doesn't reject whitespace-only strings — min(1) on " " passes Zod; service trimOrNull turns " " into null → assignments.first_name = null → UPDATE sets first_name = NULL → violates NOT NULL → DB error, not ContactValidationError. Let me check: patch `firstName: "   "` passes zod (min 1). In the loop: value = "   ", typeof string → normalized = trimOrNull("   ") = null. Compare null === current.first_name ("Erika")? No → assignment first_name = null, changedFields includes firstName. Then nameChanged: nextFirstName = null ?? current.first_name — WAIT: `const firstName = assignments.first_name as string | undefined;` — assignments.first_name is null, not undefined! `firstName ?? current.first_name` — null ?? → null is nullish, so `null ?? current.first_name` = current.first_name. Hmm! `??` coalesces null too. So nextFirstName = "Erika" (current). Then assignments.first_name = nextFirstName = "Erika" — overwrites the null. So whitespace-only firstName silently becomes current value, but changedFields already includes "firstName"... then display_name recomputed = "Erika Mustermann" (unchanged), and the UPDATE runs with first_name = "Erika" (unchanged) + revision+1 + event with changedFields ["firstName"] — a phantom change! Revision bumped, event emitted, but nothing actually changed. Edge case: violates "Kein echter Feldwechsel: kein Revisions-Bump" logic for the whitespace-only-name input. Impact: minor — a client sending firstName " " gets a phantom revision bump + event claiming firstName changed. That's a correctness bug in change detection. Severity P2 (no data corruption; revision burn + misleading event payload). Fix: validate nameSchema with trim-nonempty refine, or treat trimOrNull→null for NOT NULL columns as validation error.

    Actually wait — more precise: `trimOrNull` returns null for whitespace; the null then flows through nameChanged coalescing back to current — so DB never sees NULL. The net effect: changedFields contains firstName (phantom), display_name rewritten to same value, revision+1, event+audit written. P2.

    Also consider `salutation: null` patch — allowed (nullable().optional()). normalized = null (not string → `value as boolean | null` → null). Compare current.salutation e.g. "female" → assignment null. Fine. And CHECK allows null. But the B2B invariant: setting salutation 'business' without isBusiness=true in the same patch when current is_business=false → DB CHECK violation → raw error. E.g., patch {salutation:"business"} alone: service doesn't auto-set is_business nor validate the invariant in Zod. Contract test M114-CONTRACT-01 tests {salutation:"business", isBusiness:true} passes — but doesn't test that {salutation:"business"} alone is REJECTED at contract level. It isn't — it passes Zod and fails at DB CHECK. Again the contract-vs-DB divergence → generic DB error instead of `invalid`. Spec M114-CONTRACT-01: "Anrede-Enum + B2B-Invariante (gültig/ungültig)" — "ungültig" tested only for unknown enum value "dr", not for invariant violation. P2 (same class as PLZ): invariant not enforced at contract; DB fail-closed catches it but with unmapped error. I can merge these into one P2 finding: cross-field invariants (B2B, PLZ-DE) only at DB level; CHECK violations surface as unclassified errors, not `invalid`; tests codify the weaker behavior. Given the spec explicitly lists those cases under contract tests, I'd arguably make it P1 for spec-conformance... Let me weigh: The spec says M114-CONTRACT-01 tests "gültig/ungültig" for the invariant; the DB CHECK does enforce the invariant (fail-closed, no bad state persists). The gap is error quality, not integrity. P2 with clear fix (superRefine on patch for salutation/isBusiness pair + postal/country pair). But the consent CHECK absence is an integrity-gap relative to spec (no enforcement anywhere for consent⇒version). That stays P1.

    Hmm, actually also — the UPDATE failing with CHECK violation after advisory lock etc. — transaction rolls back, no partial state, no event. Fail-closed. Fine.

  **Revision-CAS**: The service checks `current.revision !== expectedRevision → Conflict` after `SELECT ... FOR UPDATE`, then UPDATE with `and revision = expectedRevision and deleted_at is null` returning revision; no row → Conflict. Double protection; under FOR UPDATE row lock the CAS is serialized. Note: the second CAS `if (!nextRevision) throw new ContactConflictError` — but if deleted_at became set between? Can't happen under FOR UPDATE + advisory lock. OK. Note the conflict check happens BEFORE lockBoundProject — order: lockProjectForContact (FOR SHARE on project) → advisory → lockContact (FOR UPDATE) → checks → lockBoundProject (FOR UPDATE project). 

  Wait — spec §8 says order: Advisory → Contact → Project. Implementation: FIRST `lockProjectForContact` takes `FOR SHARE` on the project row BEFORE the advisory lock! That deviates from the spec's "erster Synchronisationspunkt Advisory-Lock vor allen Zeilensperren". Rationale: you need the contact_id from the project to compute the advisory key. The comment acknowledges this ("Zuerst die Projektbindung lesen ... Der frische Snapshot wird nach dem Advisory-Lock erneut geprüft."). Is FOR SHARE on project before advisory a deadlock risk? Erasure path: advisory lock first, then... what does erase_inactive_lead lock? Unknown from diff, but per spec it locks Contact → Project (FOR UPDATE presumably). Deadlock scenario: Edit holds FOR SHARE on project P, wants advisory A. Erasure holds advisory A, wants FOR UPDATE on project P. FOR UPDATE conflicts with FOR SHARE → erasure blocks waiting for edit's SHARE lock; edit blocks waiting for advisory held by erasure. → DEADLOCK between edit and erasure! 

  Hmm wait — does erasure lock the project row? The spec §8: "Zeilensperren danach in fester Reihenfolge Contact → Project (deckungsgleich mit dem Erasure-Pfad nach dessen Advisory-Lock)". So yes, erasure takes Project row lock after advisory. If erasure's project lock is FOR UPDATE (or any lock conflicting with FOR SHARE — FOR UPDATE conflicts with FOR SHARE), then:
  - Tx1 (edit): `SELECT project ... FOR SHARE` (holds SHARE on P) → `pg_advisory_xact_lock(A)` blocks (Tx2 holds A).
  - Tx2 (erasure): holds advisory A → locks contact (FOR UPDATE, ok) → locks project FOR UPDATE → blocks on Tx1's SHARE.
  Deadlock → Postgres detects and kills one with deadlock error. Not a silent corruption, but a real livelock/deadlock risk that the spec's lock contract was designed to avoid ("vor allen Zeilensperren"). The implementation violates the specified lock order in a way that reintroduces a deadlock cycle. This is exactly what R1-03/DEC-M114-10 tried to fix. Severity: P1 (race/deadlock). Fix: read project WITHOUT lock (plain SELECT, RLS-scoped) to get contact_id → advisory lock → then re-verify project binding with FOR UPDATE (lockBoundProject already re-checks `project_record.contact_id = ${contactId}` FOR UPDATE — so the pre-read can safely be lock-free). The FOR SHARE pre-lock buys nothing that lockBoundProject's FOR UPDATE re-check doesn't. So fix: drop `for share` in lockProjectForContact (make it a plain read). Also note: the "frische Snapshot wird erneut geprüft" claim — lockBoundProject does re-verify with FOR UPDATE. Good. So the FOR SHARE is both useless and harmful. P1.

  Wait, is FOR SHARE vs FOR UPDATE really conflicting? Postgres row lock modes: FOR KEY SHARE < FOR SHARE < FOR NO KEY UPDATE < FOR UPDATE. Conflicts: FOR SHARE conflicts with FOR NO KEY UPDATE and FOR UPDATE. Erasure's scrub is an UPDATE on contact; for project it likely does UPDATE project ... (the erasure updates project? The erasure probably updates project rows too — it scrubs project PII? Actually erase_inactive_lead scrubs contact, site, project? Unknown. The spec says erasure locks Contact → Project. If erasure only reads project (e.g., FOR UPDATE to block), conflicts hold.) Even if erasure takes FOR NO KEY UPDATE or FOR UPDATE on project — both conflict with FOR SHARE. And what about edit↔edit? Both take FOR SHARE on the same project — SHARE locks don't conflict with each other; then both want advisory; one blocks; holder proceeds to lockContact FOR UPDATE... fine, no deadlock (advisory serializes; the waiter's SHARE doesn't block the holder's later FOR UPDATE? Wait — holder does lockBoundProject FOR UPDATE on project P while waiter holds FOR SHARE on P! FOR UPDATE conflicts with FOR SHARE! So even edit↔edit deadlocks:

  - Tx1: SHARE(P) → advisory(A) acquired → contact FOR UPDATE → project FOR UPDATE (P) → blocks on Tx2's SHARE(P)!
  - Tx2: SHARE(P) → advisory(A) → blocks on Tx1.

  DEADLOCK. Two concurrent edits on the SAME contact deadlock deterministically! Wait wait. Tx2 holds SHARE(P) and waits for advisory. Tx1 holds advisory and waits for FOR UPDATE(P) which conflicts with Tx2's SHARE(P). Cycle → deadlock detected, one aborted with `deadlock detected` error → surfaces as unclassified error (not conflict). This breaks M114-RACE-01 ("genau ein Schreiber gewinnt; anderer Conflict ohne Teilstand") — the loser gets a deadlock error instead of ContactConflictError. Hmm, unless... `for share` in the same transaction — yes both in same tx. This is a real P1 race bug. Actually, hold on — is it a deadlock or does Tx1's FOR UPDATE just queue behind Tx2's SHARE until Tx2 commits/aborts? Tx2 never commits because it waits on advisory held by Tx1. True deadlock cycle: Tx1 waits Tx2 (row lock), Tx2 waits Tx1 (advisory). Postgres deadlock detector fires after 1s, kills one. So M114-RACE-01 as specified (loser gets clean conflict) fails; loser gets deadlock error. The gate tests: does tests/db/m114 include a RACE-01 test? The provided m114 test file has no parallel-edit test! Testmatrix §10.4 M114-RACE-01/02 — NOT present in the diff's test file. The m114 service test covers happy path, conflict (sequential), deleted, invalid, RBAC, no-op, erasure — but no concurrency tests and no RACE-02 (edit vs erasure). Spec §10.4 demands them; gate claims 66/66 green but the race tests specified are missing entirely. That's a P1 test-gap (spec'd tests absent) — and had they existed, they'd have caught the deadlock. Combined finding: P1 — Lock-Reihenfolge verletzt (FOR SHARE auf Project vor Advisory-Lock) → echter Deadlock Edit↔Edit und Edit↔Erasure; Spec §8/DEC-M114-10 verlangt Advisory als ersten Synchronisationspunkt; zudem fehlen M114-RACE-01/02-Tests. Fix: pre-read ohne Sperre (plain SELECT) oder über skalare Subquery im Advisory-Call; danach wie gehabt.

  That's the strongest finding. Good.

  **Doppel-Insert**: intake persistContact — insert with decision.contactId; dedupe logic pre-existing. The UTM fields are set on insert only ("nur wenn leer bzw. beim Create") — the diff shows them only in the `if (!decision.existing)` branch. Consistent with spec (create-only, no reprocessing). OK.

  **Namens-Splitting**: SQL `contact_name_split_v1` vs TS `contactNameSplitV1`:
  - SQL: `regexp_replace(btrim(raw), '[ \t\r\n]+', ' ', 'g')` — collapses runs of space/tab/CR/LF to single space. TS: `raw.trim().replace(/[ \t\r\n]+/gu, " ")`. 

  Difference: JS `.trim()` removes ALL Unicode whitespace (incl. NBSP, U+2028 etc.), PG `btrim` removes only spaces (default: btrim(text) trims spaces only — actually btrim(x) without chars trims ' ' only... In Postgres, `btrim(text)` removes "the longest string containing only spaces" — yes, only space characters (U+0020), NOT tabs/newlines! Wait: Postgres docs: btrim([string text [, characters text]]) — "Removes the longest string containing only characters in characters (a space by default) from the start and end of string". So btrim(' \t x \t ') with default removes only leading/trailing SPACES, leaving tabs at the edges: btrim(" \tErika\t ") → "\tErika\t"? Let's see: leading chars: ' ' then '\t' — stops at '\t'. So result "\tErika\t". Then regexp_replace '[ \t\r\n]+' → ' ' collapses the leading "\t" (after the removed space... wait the leading sequence " \t" → after btrim removes only the leading space? No: btrim removes the longest prefix consisting solely of spaces. Prefix " " then "\t" stops. So string becomes "\tErika\t". regexp collapse: "\t" → " ", trailing "\t" → " ". Result " Erika " — with leading/trailing space! Then split_part(' Erika ', ' ', 1) = '' (empty first token!). Hmm: strpos finds first space at position 1 → first_name = '' — empty! CHECK length(btrim(first_name)) between 1 and 200 → btrim('')=0 → violation → backfill UPDATE fails → migration aborts IF any existing display_name has leading/trailing tab/newline. But existing display_name values — are they trimmed at intake? Probably (intake likely btrims). DB CHECK on display_name? Existing `contact_active_identity_ck`... unknown. If any legacy row has leading tab, migration aborts — that's actually fail-safe (abort rather than corrupt), but the TS/SQL equivalence claim (M114-CONTRACT-07 pins TS behavior; M114-DB-01 verifies SQL matches TS "für repräsentative Eingaben") diverges for edge inputs: TS trim() removes \t at ends; SQL btrim doesn't. E.g. input "\tErika\tMaxi\r\nMustermann\n" — the UNIT TEST itself uses this input! TS: trim → "Erika\tMaxi\r\nMustermann" → collapse → "Erika Maxi Mustermann". SQL: btrim removes only trailing "\n"? No — btrim default removes only ' ' (space). Wait does PG btrim(text) remove only spaces or all whitespace? Confirm: PostgreSQL btrim(string text) — "removes the longest string containing only spaces from both ends". Yes, spaces only (there's trim() which same). So SQL on that test input: btrim → unchanged "\tErika\tMaxi\r\nMustermann\n" (no leading/trailing plain spaces; ends are \t and \n) → collapse runs → " Erika Maxi Mustermann " (leading \t→space, trailing \n→space) → split_part(...,1) = '' → first_name=''. DIVERGENCE between SQL and TS — and the TS unit test explicitly covers this input. M114-DB-01 supposedly verifies equivalence for "repräsentative Eingaben" — if it included the tab case, gate would be red; gate is green, so the DB test evidently doesn't cover edge whitespace. The test file tests/db/m114-contact-dataset-service.test.ts (shown) doesn't include DB-level split equivalence at all — hmm, where's M114-DB-01? Not in the shown tests! The shown m114 test file lacks: M114-DB-01 (migration/checks), M114-DB-02 (CAS — partially covered by conflict test), M114-DB-03 (consent event exactly once — covered), RACE-01/02, RBAC-02, ERASURE-02 (source-pin drift abort), and SQL/TS split equivalence. So several spec'd DB tests are absent from the diff. But the gate says 66/66 Vitest files green — green ≠ complete. The spec's own test matrix is the contract; missing tests = spec deviation. However — maybe those tests live in other files (e.g., a migration test file m114-*.test.ts not included in diff?). The diff includes only tests shown. I can only judge the diff: within the diff, M114-DB-01/M114-RACE-01/02/M114-ERASURE-02 are not present. I'll flag as P1 (fehlende Spec-Tests) or P2? Given the deadlock bug that RACE tests would have caught — the missing race tests are material. Combine with the lock finding: the P1 lock finding mentions missing RACE tests. The split-divergence: P2 (edge-case divergence between SQL and TS split for tab/newline-padded names; also Eintoken with surrounding tabs yields empty first_name → migration abort on dirty legacy data; fix: SQL use regexp_replace(raw, '^[ \t\r\n]+|[ \t\r\n]+$', '', 'g') statt btrim, oder btrim(raw, ' \t\r\n') — actually simplest: btrim(raw_name, ' \t'||chr(10)||chr(13)) — btrim with char set. Then collapse. That matches TS trim() for the ASCII whitespace class (TS also strips Unicode spaces, but acceptable).

  Hmm wait, actually there's another subtlety in SQL split: `split_part(collapsed, ' ', 1)` and CASE WHEN strpos>0 THEN btrim(substr(collapsed, strpos+1)). TS: lastName = collapsed.slice(firstSpace+1).trim(). SQL btrim again only spaces — after collapse, internal runs are single spaces, substr after first space could still have... collapse removed all runs ≥2, so remainder has no leading space? substr from strpos+1: char at strpos is ' ', remainder starts right after — next char is non-space (since runs collapsed). So no leading space; trailing space only if input ended with whitespace-run → btrim(spaces) handles single trailing space. With btrim-fixed leading, fine. OK.

  Also note `split_part` with empty string: input "" → collapsed "" → strpos=0 → ELSE branch → last_name = "" → first="",last="" → backfill writes '' → CHECK violation → abort if any display_name is empty/whitespace. Existing display_name NOT NULL but could be ''? contact_active_identity_ck requires name or email or phone — display_name could be ''. Legacy risk; migration aborts loudly (fail-safe). Acceptable but worth noting in the P2.

  **Tombstone/Deleted-Semantik**: 
  - updateContact: `if (current.deleted_at !== null) throw ContactDeletedError` — good; plus UPDATE guard `deleted_at is null`. 
  - getContactDataset: does NOT filter deleted_at! It returns the dataset even for deleted contacts, and the DTO doesn't include deleted_at (READ_COLUMNS includes deleted_at but the DTO mapping doesn't expose it). So a deleted (scrubbed) contact renders in UI as "geloescht-<uuid>" name etc. Spec: "fehlende Optionale rendern als 'Nicht hinterlegt', nie als Existenz-Orakel; Denied/NotFound ohne Contact-Leak" and M114-07 "Edit auf gelöschtem Contact fail-closed" (edit IS blocked). For READ of deleted contact: spec §1 says editor sees full dataset; the deleted marker UI handling isn't clearly specified — but the old page.tsx code displayed `detail.contact.displayName` — did the old detail query filter deleted? Unknown. The new page replaces display of contact data with ContactSection bound to dataset; for a scrubbed contact the UI would show pseudonymized name. Is that a leak? No — it's scrubbed data. But `deleted_at` not exposed in DTO means the UI cannot render a "gelöscht" state, and the edit form would be offered for a deleted contact (canWrite true), whose submit then fails with deleted_contact. Minor UX/semantics gap: dataset should probably include tombstone flag or return null for deleted. Spec §1: "Der DSGVO-Löschmarker (deleted_at) und die bestehende Erasure-Grenze bleiben bindend". Also page.tsx: `if (contactResult.dataset === null) notFound()` — null only when project/contact row missing. Hmm, what did the OLD code do for deleted contacts — `detail.contact` from loadProjectDetail presumably already exposed displayName of deleted contacts the same way (scrubbed pseudonym). So behavior parity. I'll flag as P2: getContactDataset liefert gelöschte Kontakte ohne Tombstone-Indikator; UI kann Löschzustand nicht darstellen und bietet Edit an, der erst serverseitig deleted_contact wirft. Fix: `deletedAt`/tombstone ins DTO oder null + notFound — je nach Spec-Klärung. P2.

  **Marketing-Consent/UTM**: consent boolean not patchable (intake-owned) — spec §5 allowlist includes "Consent-Felder" — ambiguous whether the boolean itself is included; the contract comment justifies exclusion. But M114-06 capability says "Marketing-Consent mit Policy-Version" editierbar and UI-Vertrag §9 says "Consent (Checkbox + Version/Text/Link, read-only bei fehlender contact.write)" — a Checkbox implies the BOOLEAN is editable in UI! But the patch excludes `granted`. Contradiction: UI spec says checkbox editable; contract excludes the boolean. If UI renders a checkbox wired to a non-patchable field, E2E-04 ("Consent ... sichtbar/editierbar") fails. Since contact-section.tsx is NOT in the diff, can't verify — but the contract clearly has no `granted` in the allowlist. That's a spec-internal contradiction the implementation resolves by exclusion; flag as P1 or P2? The spec §5 says patch allowlisted on "Consent-Felder" and the command comment explicitly says boolean is intake-owned. The API (reference) has marketingConsent create-only (not in update body) — so excluding the boolean matches the API reference (UNK-M114-02). But UI-Vertrag §9 promises an editable checkbox. Implementation can't satisfy §9's checkbox. Flag as P2 spec-inconsistency (checkbox vs. allowlist); implementation chose API-conform; spec §9 needs amendment or patch needs `granted` + consent_changed event on toggle. Note the consent_changed event: emitted when policyVersion/text/link changed — spec §7: "contact.marketing_consent_changed als eigenes Event bei Consent-Änderung (trägt Policy-Version, kein Kontakt-PII)". Implementation payload: {contactId, policyVersion} — policyVersion is not contact-PII, ok. But event fires on text/link change too — "Consent-Änderung" interpreted broadly; acceptable. However: `nextPolicyVersion` fallback — fine.

  Wait, one more: the consent event should fire when the CONSENT state changes; since granted isn't patchable, policy-version change as proxy. OK.

  **UTM in event payload**: changedFields contains patch keys only — field NAMES, no values. Matches §7. But note `changedFields` may include "firstName" even when only display-rewrite... covered above. Also the no-op detection compares `normalized === current[column]` — current from DB row; for email_secondary the current is already lowercase; normalized lowercased — good. For marketingConsentText: z.string().max(20000) — trimOrNull applied: text that is whitespace-only → null; text with leading/trailing spaces → trimmed silently (mutation of user content; fine).

  BUT — the equality check `normalized === current[columnName]`: for `marketing_consent_text` current could be long string; fine. For boolean is_business: value boolean; normalized = value; current boolean — ok. For patch value null on a column currently null: normalized null === null → skip. Good.

  One more: `assignments` uses `keyof ContactRow` — includes only mapped columns; display_name added when name changed. `updateColumns` from Object.keys(assignments) — safe, server-side map, `sql.identifier` quoting. No injection. Good.

  **Audit**: writeAudit with details = evidence {contactId, revision, changedFields} — no PII values. Matches §7. Event payload minimal. Good. But: on ContactConflictError/deleted/validation — no audit row for DENIED attempts? Spec M114-RBAC-01: "ohne Event-/Audit-Zeile" for unauthorized — requireContactWrite throws before any writes — good. Failed business outcomes (conflict) without audit is consistent with "allowed=true" entries only... pre-existing pattern presumably. Fine.

  **Activity read model**: §7 "Projektaktivität (redigiert): festes deutsches Label 'Kontaktdaten geändert'". The diff doesn't show changes to the activity read model (modules/... activity) — is contact.updated mapped to the label? Not in diff. If the activity read model has an allowlist of event types and contact.updated isn't added, the event exists but never appears in project activity → spec §7 partially unmet. Cannot verify from diff → note as P2 "nicht verifizierbar im Diff: Activity-Label-Mapping für contact.updated". Actually the page load notePage etc. — no. Flag as P2/unverifizierbar.

  ### 3. Erasure

  - Quellgepinnt: SHA-256 of prosrc checked (`891d...`), anchor checked, abort otherwise. Matches ADR/M111B pattern. Good.
  - 24-Monats-Eligibility: test sets updated_at 25 months ago on contact/site

---
> Hinweis Root: Kimi-Ausgabe abgeschnitten (CLI-Stream eingefroren); Befunde vollständig geerntet und an den Implementierer zur Schließung übergeben (2026-09-03).
